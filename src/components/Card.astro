---
interface Props {
    class?: string;
}

const { class: classes } = Astro.props;
---

<div class:list={["px-5 py-7 bg-accent/10 rounded-2xl transition-transform hover:scale-105 ", classes]}>
    <slot />
</div>